import TemplateContent from '@/components/parts/TemplateContent';
//Commented out: there is no available hooks in this path
// import { useHooks } from '@/components/templates/Dashboard/hooks';
// import LeadList from '@/components/templates/Leads/LeadList';

const Dashboard = () => {
  // const { leads, page, setPage } = useHooks();

  return (
    <TemplateContent>
      {/* <LeadList leads={leads} page={page} setPage={setPage} /> */}
      <></>
    </TemplateContent>
  );
};

export default Dashboard;
